{
  "name": "liveframe-plugin",
  "version": "1.0.1",
  "description": "blockbench plugin for exporting animations to LFA (LiveFrame Animation)",
  "main": "src/plugin.js",
  "scripts": {
    "build": "node scripts/build.js"
  },
  "private": true,
  "devDependencies": {
    "blockbench-types": "^5.0.6",
    "esbuild": "^0.27.4",
    "archiver": "^6.0.0"
  }
}
